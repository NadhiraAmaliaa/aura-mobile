import 'package:flutter/material.dart';

/// Core brand colors. Placeholder palette until the final brand kit is
/// provided; a green seed reflects the PTPN (agribusiness) identity.
abstract final class AppColors {
  static const Color seed = Color(0xFF1B5E20);
}
